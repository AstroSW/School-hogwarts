SELECT * FROM student
WHERE age BETWEEN 10 AND 13;

SELECT name FROM student;

SELECT * FROM student
WHERE LOWER(name) LIKE '%и%';

SELECT * FROM student
WHERE age > id;

SELECT * FROM student
ORDER BY age;